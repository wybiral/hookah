// Package protocols implements all builtin hookah protocols.
package protocols

// queueSize is number of messages to queue for outgoing channels.
const queueSize = 10

// bufferSize is number of bytes in read buffer.
const bufferSize = 8 * 1024
